# Base URL of the NestJS API.
#  - API_URL is read on the server (Server Components, Server Actions).
#  - NEXT_PUBLIC_API_URL is exposed to the browser; only used for client-side
#    requests that don't go through Server Actions.
API_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001
